// --- Interrupt Service Routine ---
// Verifies 'interrupt' attribute parsing
interrupt void handle_keyboard() {
    // Standard Moksha logic goes here!
    // No need for pushaq, popaq, or iretq since LLVM handles it automatically via x86_intrcc
}

// --- Naked Stub (Bootloaders/Syscalls) ---
// Verifies 'naked' attribute (no prologue/epilogue generated by backend)
naked void bootstrap_entry() {
    asm("mov %rax, %rsp");
    asm("jmp kernel_main");
}
