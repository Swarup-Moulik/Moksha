void riskyOperation() {
    // Top-level statements inside function
    if (true) throw "Something went wrong"
}

// Defer at top level (implicitly inside synthetic main)
defer print("Cleaning up...")

try {
    riskyOperation()
} catch (string e) {
    print(e)
} finally {
    print("Operation attempted.")
}
