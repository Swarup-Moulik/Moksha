ref class Node {
    public int[] values;
    public Node? next;
    constructor() {
        this.values = [1, 2, 3];
        this.next = null;
    }
}

Node? processNode(int val) { return new Node(); }

Node root = new Node();
bool flag = true;

int complexResult = (root?.values[0] |> processNode()?.next != null) ? 100 : 200;

print(complexResult);
