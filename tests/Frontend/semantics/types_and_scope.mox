// Explicit Type
int x = 10;

// Scoping & Shadowing
{
  int x = 20; // Allowed: shadows outer x
  print(x);   // Prints 20
}
print(x); // Prints 10

// Const correctness
const double PI = 3.14;
double area = PI * 2.0;
