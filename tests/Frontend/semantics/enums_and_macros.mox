// Enum Definition
enum Color {
    Red,
    Green,
    Blue = 10,
    Alpha
}

// Macro Definition
macro swap(a, b) {
    int temp = a
    a = b
    b = temp
}

// Usage
Color c = Color.Green
int x = 10
int y = 20

// Macro Expansion check
swap(x, y)
