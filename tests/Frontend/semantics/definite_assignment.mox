// Valid: Control flow guarantees assignment
int x;
if (true) {
    x = 10;
} else {
    x = 20;
}
print(x);
// Safe: 'x' is definitely assigned in all paths

// Valid: Loop assignment with guaranteed entry
int y;
do {
    y = 5;
    break;
} while (true);

print(y); // Safe
