// Shared memory modifier
shared int globalCounter = 0;
int localCounter = 0;

mut int protectedValue = 10;

new thread(() => {
    // globalCounter is shared, so this is legal
    globalCounter = globalCounter + 1;

    atomic_add(&protectedValue, 1);
});

new weak thread(() => {
    print("Weak thread execution");
});
