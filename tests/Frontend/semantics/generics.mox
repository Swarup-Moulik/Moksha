// Generic Definition
generic Box<T> {
    T value;
    void update(T newValue) {
        this.value = newValue;
    }
}

// Concrete Instantiation
Box<int> intBox = new Box<int>();
intBox.update(100);

Box<string> strBox; // Should also work
