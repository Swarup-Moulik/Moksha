// ERROR: TypeChecker should flag that "admin" (string) cannot be assigned to 'int'
void badDefault(int level = "admin") {
}

void processData(string data, int retries = 3, int timeout = 5000) {
}

void main() {
    // ERROR: Even with defaults, the required 'data' argument is missing
    processData();

    // ERROR: Provided too many arguments (max is 3)
    processData("payload", 5, 10000, "extraArg");
}
