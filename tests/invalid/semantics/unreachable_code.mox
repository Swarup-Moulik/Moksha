int process() {
    return 42;
    print("This is dead code"); // ERROR: Unreachable statement
}

int failProcess() {
    throw "Fatal";
    int x = 10; // ERROR: Unreachable after throw
}

enum State { Start, Processing, End }

State current = State.Start;

// ERROR: Non-exhaustive switch statement. Missing 'End' case.
switch (current) {
    case State.Start: print("Starting");
    case State.Processing: print("Working");
}
