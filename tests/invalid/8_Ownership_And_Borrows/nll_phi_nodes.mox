void test_phi(bool cond) {
    int a = 1;
    int b = 2;

    // ptr could point to 'a' OR 'b'. The borrow checker must lock BOTH.
    *view int ptr = cond ? &a : &b;

    *mut int m = &a; // ERROR: 'a' might be actively borrowed by 'ptr'!
    *m = 5;

    print(*ptr);
}
