// A generic class that explicitly requires ARC-managed (shared) types
generic Container<shared T> {
    T item;
}

void test_generics() {
    int x = 10;
    *view int v = &x;

    // HIR must reject passing a borrowed view into a struct that requires 'shared' ownership
    Container<*view int> c = new Container<*view int>();
}
