closure() -> void create_dangling_closure() {
    int local_data = 100;
    *view int v = &local_data;

    // Capture the view pointer by value.
    // The closure now holds a physical memory address pointing to the stack.
    closure() -> void bad_closure = () => {
        print(*v);
    };

    return bad_closure; // Error: local_data is destroyed here!
}


    closure() -> void boom = create_dangling_closure();

    // FATAL: The NLL Borrow Checker MUST catch this!
    // The closure holds a *view to a destroyed stack frame.
    boom();
