void dangerous_func() {}

void test() {
    try {
        dangerous_func() // Should lower to 'invoke'
    } catch (int e) {
        // ...
    }
    dangerous_func() // Should lower to 'call'
}
