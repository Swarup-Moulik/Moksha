ref class Node {
        // Empty class for safe elision
    }

    void test_phi_merge(bool cond) {
        Node n1 = new Node();
        Node n2 = new Node();

        // The naive inserter will retain the result of the Phi node.
        // The analyzer must see it's never used or escaped and elide it entirely.
        Node result = cond ? n1 : n2;
    }
