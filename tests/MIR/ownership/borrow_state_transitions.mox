void test_borrow_transitions() {
    int x = 10

    *view int v1 = &x
    *view int v2 = &x
    int read_val = *v1 + *v2

    // Test: *mut is allowed because previous views are dead
    *mut int m1 = &x
    *m1 = 20
}
