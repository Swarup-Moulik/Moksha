void test_chain() {
    int data = 10;
    *mut int m1 = &data;

    // Reborrow from the reference itself
    *view int v = &*m1;

    int read = *v;
    // v dies here.

    *m1 = 20; // Safe to use m1 again!
}
