void test_exception() {
    int data = 42;
    try {
        *mut int m = &data;
        throw 1;
        // 'm' dies on the unwind edge!
    } catch (int e) {
        *mut int m2 = &data; // SUCCESS: previous borrow was destroyed by the throw.
        *m2 = 99;
    }
}
